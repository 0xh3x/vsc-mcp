{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "vsc-mcp Extension",
      "type": "extensionHost",
      "request": "launch",
      // "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "pnpm: build-extension"
    }
  ]
}
